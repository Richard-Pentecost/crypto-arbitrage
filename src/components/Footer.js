import React from 'react';
import classes from '../styles/Footer.module.scss';

const Footer = () => {
  return (
    <div className={classes.footer}>
      <div className={classes.footer__title}>Footer</div>
    </div>
  );
};

export default Footer;
